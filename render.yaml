services:
  - type: web
    name: n8n
    env: node
    nodeVersion: 22
    buildCommand: yarn install --frozen-lockfile
    startCommand: n8n start
    envVars:
      - key: N8N_PORT
        value: 5678
